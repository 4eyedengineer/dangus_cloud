# Dangus Cloud - Environment Variables
# =====================================
# Copy this file to .env and fill in your values
#
# For local development with Tilt:
#   1. cp .env.example .env
#   2. Fill in your GitHub OAuth credentials
#   3. tilt up

# GitHub OAuth (Required for login)
# Create an OAuth app at https://github.com/settings/developers
# - Homepage URL: http://localhost:5173
# - Authorization callback URL: http://localhost:3001/auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Security Keys (Optional - defaults provided for dev)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=dev-encryption-key-32-chars-long!
SESSION_SECRET=dev-session-secret-32-chars-long!

# Harbor Registry (Optional - for deployments)
HARBOR_REGISTRY=harbor.192.168.1.124.nip.io
